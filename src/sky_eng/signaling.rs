use crate::collector::import_all::BTickData;
use crate::types::*;
use super::*;

impl SkyEng {
    pub fn set_signals(&mut self, tick: &BTickData, sf: &mut SFrame) -> Option<ActionSignal> {


        None
    }
}