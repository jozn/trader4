run webpack:
npm run build


@todo:
1- add hide/show lines in main chart >> add a list of lines needed and render main chart based on
    that list: ex: wave1 in list spec > if in list, then render - onChange of checkbox rerender the chart